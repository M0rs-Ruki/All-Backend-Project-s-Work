<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Registration Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: #1a202c;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }
        .form-container {
            max-width: 400px;
        }
        input:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(66, 153, 225, 0.5);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4">
    <div class="form-container bg-white rounded-lg shadow-lg p-6 w-full">
        <div class="flex justify-center">
        <div class="w-20 h-20 rounded-md overflow-hidden mb-8">
            <img src="/images/uploads/<%= user.profilePic %>" alt="Profile Picture" class="w-full h-full object-cover rounded-md" />
        </div>
        </div>
        <h1 class="text-2xl font-bold text-gray-800 mb-6 text-center">Upload  File</h1>
        <form action="/upload" method="POST" class="space-y-4" enctype="multipart/form-data">
            <div>
                <input type="file" name="image" class="w-full">
            </div>
            
            <div>
                <input 
                    type="submit" 
                    value="Upload File"
                    class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg cursor-pointer transition duration-200"
                >
            </div>
        </form>
    </div>
</body>
</html>